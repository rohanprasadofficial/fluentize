export const Switch = (toggled, state) => `<Switch defaultCheck={${toggled}}  state={${state}} />`;
export const Avatar = (size, style, presence, activityRing) =>
    `<Avatar size={${size}} style={${style}} presence={${presence}} activityRing={${activityRing}}`;
